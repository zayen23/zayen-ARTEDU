{{ form_start(form, {'attr': {'class': 'needs-validation', 'novalidate': true}}) }}
    <div class="mb-3">
        {{ form_row(form.description, {'attr': {'class': 'form-control', 'rows': 4}}) }}
    </div>
    <div class="row">
        <div class="col-md-6 mb-3">
            {{ form_row(form.sponsor, {'attr': {'class': 'form-select'}}) }}
        </div>
        <div class="col-md-6 mb-3">
            {{ form_row(form.amount, {'attr': {'class': 'form-control', 'step': '0.01'}}) }}
        </div>
    </div>
    <div class="row">
        <div class="col-md-6 mb-3">
            {{ form_row(form.type, {'attr': {'class': 'form-select'}}) }}
        </div>
        <div class="col-md-6 mb-3">
            {{ form_row(form.sponsorshipType, {'attr': {'class': 'form-select'}}) }}
        </div>
    </div>

    <div class="mt-3">
        <button type="submit" class="btn btn-primary">{{ button_label|default('Enregistrer') }}</button>
        <a href="{{ path('app_sponsorship_index') }}" class="btn btn-secondary">Annuler</a>
    </div>
{{ form_end(form) }}

<script>
document.addEventListener('DOMContentLoaded', function() {
    const descriptionTextarea = document.querySelector('textarea[name*="[description]"]');
    const amountInput = document.querySelector('input[name*="[amount]"]');
    
    // Validation de la description
    if (descriptionTextarea) {
        descriptionTextarea.addEventListener('blur', function() {
            const value = this.value.trim();
            if (value && (value.length < 10 || value.length > 2000)) {
                this.setCustomValidity('La description doit contenir entre 10 et 2000 caractères');
            } else {
                this.setCustomValidity('');
            }
        });
    }
    
    // Validation du montant
    if (amountInput) {
        amountInput.addEventListener('input', function() {
            // S'assurer que seuls les chiffres et le point décimal sont acceptés
            this.value = this.value.replace(/[^0-9.]/g, '');
            
            // S'assurer qu'il n'y a qu'un seul point décimal
            const parts = this.value.split('.');
            if (parts.length > 2) {
                this.value = parts[0] + '.' + parts.slice(1).join('');
            }
            
            // Limiter à 2 décimales
            if (parts.length === 2 && parts[1].length > 2) {
                this.value = parts[0] + '.' + parts[1].substring(0, 2);
            }
        });
        
        amountInput.addEventListener('blur', function() {
            const value = parseFloat(this.value);
            if (isNaN(value) || value <= 0) {
                this.setCustomValidity('Le montant doit être un nombre positif');
            } else if (value > 9999999.99) {
                this.setCustomValidity('Le montant ne peut pas dépasser 9999999.99');
            } else {
                this.setCustomValidity('');
            }
        });
    }
});
</script>
